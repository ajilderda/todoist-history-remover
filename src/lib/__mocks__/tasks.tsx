import * as items from '../../../mocks/tasks.get.json';
import * as completedItems from '../../../mocks/completedItems.get.json';

export const getItems = () => {
  return items;
}

export const getCompletedItems = () => {
  return completedItems;
}
